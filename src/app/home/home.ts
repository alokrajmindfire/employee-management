import { Component } from '@angular/core';
import { Header } from '../component/header/header';

@Component({
  selector: 'app-home',
  imports: [Header],
  templateUrl: './home.html',
})
export class Home {}
